# No API keys needed - we use local Whisper models!
# Just make sure you have whisper.cpp, Python whisper, or faster-whisper installed

# Optional: Configure model path
# WHISPER_MODEL_PATH=/path/to/models/ggml-base.bin

# Optional: Set language (default: en)
# WHISPER_LANGUAGE=en
